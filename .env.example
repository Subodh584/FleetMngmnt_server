# Django
DJANGO_SECRET_KEY=your-secret-key-here
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=*

# Database (Supabase PostgreSQL)
DATABASE_URL=postgresql://postgres:YOUR-PASSWORD@db.ibkarmajufdlfirdfrki.supabase.co:5432/postgres

# Redis (optional – falls back to in-memory channel layer if not set)
REDIS_URL=redis://127.0.0.1:6379/0

# CORS (comma-separated origins, only used when DEBUG=False)
CORS_ALLOWED_ORIGINS=https://yourdomain.com
